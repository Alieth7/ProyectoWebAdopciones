<?php

namespace App\Http\Controllers;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

class ControllerAdmin extends Controller
{
    /**controladores de admin-------> ya no lo usamos por que seran vistas protegidas que mostrara su ruta si algun usuario tiene acceso a ella, la misma ruta muestra la vista
    public function metodoinicio(){
     return view('admin.vistaadmin');// inicio de administrador(en lugar de poner{{ rout es('')} },mejor este alias) }  */ 
}
